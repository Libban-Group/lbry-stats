{
  "name": "lbry-stats",
  "module": "index.js",
  "type": "module",
  "scripts": {
    "start": "bun run build && bun run index.js",
    "build": "astro build",
    "dev": "DEV=true nodemon --ext '*' --exec 'bun run build && bun run index.js'"
  },
  "nodemonConfig": {
    "ignore": ["/dist/"]
  },
  "devDependencies": {
    "bun-types": "^0.7.0"
  },
  "dependencies": {
    "@astrojs/mdx": "^0.19.7",
    "astro": "^2.10.1",
    "dotenv": "^16.3.1",
    "hono": "^3.3.2"
  }
}
