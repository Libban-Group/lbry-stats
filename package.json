{
  "name": "lbry-stats",
  "module": "index.js",
  "type": "module",
  "scripts": {
    "start": "bun run build && bun run index.js",
    "build": "astro build",
    "dev": "DEV=true nodemon --ext '*' --exec 'bun run build && bun run index.js'"
  },
  "nodemonConfig": {
    "ignore": ["/dist/"]
  },
  "devDependencies": {
    "bun-types": "^1.0.4"
  },
  "dependencies": {
    "@astrojs/mdx": "^1.1.1",
    "astro": "^3.2.4",
    "dotenv": "^16.3.1",
    "hono": "^3.7.5"
  }
}
