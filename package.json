{
  "name": "lbry-stats",
  "module": "index.js",
  "type": "module",
  "scripts": {
    "start": "bun run build && bun run index.js",
    "build": "astro build",
    "dev": "DEV=true bun run build && bun --watch run index.js"
  },
  "nodemonConfig": {
    "ignore": ["/dist/"]
  },
  "devDependencies": {
    "bun-types": "^1.0.4"
  },
  "dependencies": {
    "@astrojs/mdx": "^2.0.0",
    "astro": "^4.0.3",
    "dotenv": "^16.3.1",
    "hono": "^3.11.12"
  }
}
